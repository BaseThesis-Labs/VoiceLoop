# voice-evals requirements
# Install: pip install -r requirements.txt
# For a minimal install (core only): pip install -e .
# For specific extras: pip install -e ".[asr]" or pip install -e ".[tts]"
# For everything: pip install -e ".[full,test]"

# ── Core ─────────────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
librosa>=0.10.0

# ── ASR metrics (WER, CER, SeMaScore, SAER, ASD) ────────────────────
openai-whisper>=20231117
transformers>=4.30.0
sentence-transformers>=2.2.0
jiwer>=3.0.0
tiktoken>=0.5.0

# ── TTS metrics (UTMOS, prosody, speaker similarity) ─────────────────
torch>=2.0.0
torchaudio>=2.0.0

# ── Agent metrics (task success, coherence via LLM-as-judge) ─────────
openai>=1.0.0

# ── Testing ──────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-timeout>=2.2.0

# ── Optional (uncomment if needed) ───────────────────────────────────
# pyannote.audio>=3.1.0    # speaker diarization (requires HF token)
# funasr>=1.0.0            # emotion detection via Emotion2Vec
# onnxruntime>=1.16.0      # DNSMOS quality scoring
